export enum ExperienceType {
    Hands = 'hands',
    Pendulum = 'pendulum',
}

export enum TabsValues {
    Scheduler = "scheduler",
    Participants = "participants",
    Experiences = "experiences",
}
